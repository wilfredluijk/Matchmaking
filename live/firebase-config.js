// Replace the placeholder values with your Firebase project's web config.
// Find these at: Firebase Console → Project settings → Your apps → SDK setup.
// All values here are public-safe; security comes from Firestore rules.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
